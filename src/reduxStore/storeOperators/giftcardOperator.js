// import {bindAllClassMethodsToThis} from 'util/bindAllClassMethodsToThis';

export const MODAL_IDS = {
  giftcardBalanceModalId: 'giftcardOperator_giftcardBalanceModalId'
};

/* operator intended to move all gift card related opetators in here,
as of now only used to declare the constant to open the check balance form modal
let previous = null;
export function getGiftcardOperator (store) {
  if (!previous || previous.store !== store) {
    previous = new GiftcardOperator(store);
  }
  return previous;
}

class GiftcardOperator {
  constructor (store) {
    this.store = store;
    bindAllClassMethodsToThis(this);
  }
}
*/
